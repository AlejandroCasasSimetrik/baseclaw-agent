/**
 * Level 7 — TTS Provider Types
 *
 * Re-exports TTS-related types from the central voice types module.
 */

export type {
    TTSProvider,
    TTSResult,
    VoiceInfo,
    VoiceConfig,
} from "../types.js";
